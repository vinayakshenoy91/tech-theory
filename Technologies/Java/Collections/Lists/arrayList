- Give  number of items in arraylist as this will improve perf. new ArrayList<>(50000)
- Sorting is faster compared to linked list

List is an interface (ADT) and ArrayList is concrete implementation of it.


Object[] o = arrayListName.toArray();

arrayList.add(0,x);
arrayList.remove(index);
arrayList.contains('Hello')

